--- a/include/config.h
+++ b/include/config.h
@@ -465,14 +465,12 @@
 #define STICKY_OBJECTS
 #define PREADJUST /* See preadjust.diff */
 
-#ifndef NO_VOICE /* compile-time option enabled--skip this */
+#ifndef NO_VOICE /* compile-time option: define NO_VOICE to disable voice support */
 #define VOICE_ENABLED
 #define MAX_VOICE_EXCEPTIONS 200 // Adjust as needed
-extern char voice_exceptions[MAX_VOICE_EXCEPTIONS][BUFSZ]; // New array for VOICE_FORCE patterns
+extern char voice_exceptions[MAX_VOICE_EXCEPTIONS][BUFSZ];
 extern int num_voice_exceptions; // New counter for VOICE_FORCE patterns
-#define MAX_VOICE_FORCES 200     // New limit for VOICE_FORCE patterns; change also in options.c
-extern char voice_forces[MAX_VOICE_FORCES][BUFSZ]; // New array for VOICE_FORCE patterns
-extern int num_voice_forces;                       // New counter for VOICE_FORCE patterns
+#define MAX_VOICE_FORCES 200
+extern char voice_forces[MAX_VOICE_FORCES][BUFSZ];
+extern int num_voice_forces;
 #endif
